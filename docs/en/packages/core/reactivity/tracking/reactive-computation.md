# `TReactiveComputation<T>`

`TReactiveComputation<T>` represents an internal zero-argument operation executed by the tracking context. It preserves the operation's return type for both tracked collection and untracked execution.

See [`IReactiveTracker`](reactive-tracker.md).
