/** @description Immutable local identifiers reserved for deterministic generated compiler bindings. */
export const lilyReservedIdentifiers: readonly string[] = Object.freeze([
    "Template",
    "$state",
    "$template",
]);
