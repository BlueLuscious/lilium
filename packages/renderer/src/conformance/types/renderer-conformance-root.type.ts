/** @description External object root shape shared by universal Renderer conformance scenarios. */
export type RendererConformanceRootType = {
    /** @description Stable diagnostic name with no protocol meaning. */
    readonly name: string;
};
