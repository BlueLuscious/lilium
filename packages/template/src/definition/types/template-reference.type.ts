/**
 * @description Definition-local structural ordinal assigned during template normalization.
 * @remarks References identify declarations only inside one template definition. They are not
 * application keys, global identifiers, host handles, or runtime occurrence identities.
 */
export type TemplateReferenceType = number;
