/**
 * @description Synchronous operation executed with a scope as active owner.
 * @returns `undefined` after scoped resource creation completes.
 */
export type ScopeFunctionType = () => undefined;
