/** @description Externally owned logical root accepted by the private Console adapter. */
export type ConsoleRootType = Readonly<{
    /** @description Optional deterministic diagnostic name with no identity semantics. */
    name?: string;
}>;
