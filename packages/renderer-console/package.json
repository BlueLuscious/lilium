{
  "name": "@lilium/renderer-console",
  "version": "0.1.0",
  "description": "Private logical host adapter and conformance infrastructure for Lilium Renderer.",
  "private": true,
  "author": "BlueLuscious",
  "license": "ISC",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "pnpm run clean && tsc",
    "clean": "node ../../tooling/cli/clean-package-distribution.mjs",
    "test": "pnpm --filter @lilium/core run build && pnpm --filter @lilium/component run build && pnpm --filter @lilium/template run build && pnpm --filter @lilium/renderer run build && pnpm run build && pnpm run test:types && pnpm run test:runtime",
    "test:runtime": "tsx --test tests/runtime/**/*.test.ts",
    "test:types": "tsc -p tsconfig.test.json",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@lilium/renderer": "workspace:*"
  },
  "devDependencies": {
    "@lilium/core": "workspace:*",
    "@lilium/template": "workspace:*"
  },
  "keywords": [
    "conformance",
    "lilium",
    "renderer",
    "testing"
  ]
}
